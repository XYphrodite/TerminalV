# Как пользоваться TerminalV

## Окно

Слева — список сессий, справа — терминал выбранной сессии или несколько связанных панелей. При первом запуске создаётся новая сессия; в дальнейшем восстанавливается сохранённый список. Оболочка по умолчанию — PowerShell 7 (`pwsh.exe`), если его нет — Windows PowerShell.

Переопределить оболочку:

```powershell
$env:TERMINALV_SHELL = 'C:\Windows\System32\cmd.exe'
TerminalV
```

Первая сессия стартует из домашней папки; последующие новые вкладки наследуют рабочую папку активной сессии.

## Вкладки

- Кнопка **Новая вкладка** или `Ctrl+Shift+T`
- Клик по вкладке — переключение
- Перетаскивание вкладки мышью — изменить порядок вкладок
- Крестик, средняя кнопка мыши или `Ctrl+Shift+W` — закрыть
- Двойной клик по названию — переименовать, `Enter` сохранить, `Esc` отменить
- Неактивная вкладка с новым выводом помечается индикатором

При закрытии живой сессии появляется предупреждение с её названием и кнопками **Завершить сессию** / **Отмена**.
Крестик, средняя кнопка мыши и `Ctrl+Shift+W` используют одинаковое подтверждение; сочетание работает и при русской раскладке.
По умолчанию выбрана **Отмена**. `Esc` отменяет закрытие и возвращает фокус в терминал или в поле поиска, если оно было активно.

До подтверждения сессия продолжает работать. **Завершить сессию** закрывает только выбранную вкладку и останавливает её процессы; несохранённые данные могут быть потеряны.
Предупреждение показывается для любой живой сессии, даже если PowerShell просто ожидает команду. Уже завершённая сессия закрывается без предупреждения.
Если процесс завершился во время подтверждения, предупреждение отменяется, а вкладка остаётся с выводом и кодом завершения.

Закрытие всего окна по-прежнему сохраняет работающие фоновые сессии. Чтобы убрать вкладку, не останавливая процесс, используйте отдельное действие **Скрыть, оставив работать**. Подтверждённое закрытие крестиком именно завершает сессию.

## Управление сессиями

Нажмите **⋯** у вкладки или правую кнопку мыши по её названию. С клавиатуры: перейдите фокусом на строку вкладки и нажмите `Shift+F10`.

- **Группа проекта** — введите новое название или выберите существующее из подсказок. Пустое поле возвращает вкладку в раздел «Без группы». Пустые группы не показываются.
- **Цветовая метка** — пять цветов на выбор или «Без метки». Цвет отображается полоской у вкладки и сохраняется при переключении темы.
- **Закрепить вверху группы** — помечает вкладку звёздочкой и поднимает над остальными вкладками своей группы.
- **Без звука для этой сессии** — отключает звуковые уведомления BEL только для выбранной сессии, сохраняя визуальные отметки.
- **Сохранить** применяет настройки. **Отмена** и `Esc` оставляют всё как было.

Перетаскивание сохраняет порядок. При переносе на вкладку другой группы сессия получает её группу; закрепление также становится таким же, как у целевой вкладки, чтобы место вставки совпадало с видимым порядком. Новая вкладка наследует группу и текущую папку активной сессии, но не её закрепление и цвет. `Ctrl+Tab` и `Alt+1` … `Alt+9` следуют порядку открытых вкладок на экране и пропускают скрытые.

Поле **Найти сессию…** фильтрует список по названию и группе, без учёта регистра, включая кириллицу. `Enter` открывает первый результат; `Esc` сбрасывает фильтр и возвращает фокус терминалу. Фильтр не завершает и не скрывает сессии, а ввод в него не отправляется в PowerShell. Поиск текста внутри терминала остаётся отдельной функцией — `Ctrl+Shift+F`.

### Скрытые сессии

В диалоге **⋯** нажмите **Скрыть, оставив работать**. Настройки группы, цвета и закрепления при этом тоже сохраняются. Вкладка исчезнет из открытого списка, но её PowerShell и запущенные программы продолжат работать в фоне. Для уже завершённой сессии кнопка называется **Сохранить и скрыть**.

Кнопка **Скрытые · N** над списком показывает скрытые сессии; поле поиска работает и в этом списке. Нажмите на нужную строку или **↩**, чтобы вернуть вкладку с прежним процессом и выводом. Для завершения сначала верните вкладку, затем закройте обычным крестиком с подтверждением.

Можно скрыть все вкладки: новая оболочка автоматически не запускается. После закрытия и повторного открытия окна скрытые сессии сохраняются; TerminalV подключается к их прежним процессам, а не запускает копии. Скрытые терминалы продолжают принимать вывод, поэтому скрытие упорядочивает список, но не освобождает всю память их экранных буферов.

После перезагрузки Windows или завершения процесса остаётся сохранённый экран. При возвращении такой вкладки новая оболочка не запускается автоматически: для этого есть кнопка **Перезапустить**. Группы, метки, закрепление, порядок и видимость сохраняются в SQLite; старая база обновляется автоматически без удаления существующих вкладок.

## Профили запуска

Профили доступны с v0.5.0. В исходниках после v0.5.1 кнопка **Профили и оболочки** под новой вкладкой открывает меню быстрого запуска:

- Нажмите на сохранённый профиль или **Запустить** в его строке — сразу откроется новая сессия. Редактор и повторное сохранение не нужны.
- Кнопка с карандашом **Изменить** рядом с профилем открывает отдельный редактор. **Сохранить** записывает изменения, но ничего не запускает.
- **Создать профиль** открывает пустой редактор: название, оболочка, цветовая метка, папка и необязательная стартовая команда. **Сохранить** только добавляет профиль. **Создать и запустить** сначала дожидается записи в базу и затем открывает сессию; повторные нажатия не запускают вторую копию.
- В разделе **Оболочки** доступны обнаруженные Windows PowerShell, PowerShell 7, cmd и дистрибутивы WSL. Их запуск не создаёт сохранённый профиль.
- Меню поддерживает стрелки, `Home`/`End`, `Tab`/`Shift+Tab`; `Esc` или клик вне меню закрывает его. Терминальные сочетания клавиш за меню не выполняются.

В выпущенной v0.5.1 пока используется прежний редактор со списком профилей и кнопкой **Сохранить и запустить**.

Оболочки: **Автоматически** (обычный выбор TerminalV, включая `TERMINALV_SHELL`), **Windows PowerShell**, **PowerShell 7** и **Командная строка** (cmd). Явный выбор не зависит от `TERMINALV_SHELL`; если PowerShell 7 не установлен, появится ошибка, а другая оболочка вместо него не запустится. Произвольные аргументы оболочки в этой версии редактора не настраиваются.

Папка задаётся полным путём без внешних кавычек; переменные окружения в нём не раскрываются. Пустое поле означает домашнюю папку. Поддерживаются пробелы и кириллица. В отличие от обычной новой вкладки, профиль с недоступной папкой **не запускается**: его команда не должна случайно выполниться в другом каталоге. Сетевой UNC-путь доступен для PowerShell, но не для cmd.

PowerShell поддерживает многострочную стартовую команду, максимум 4096 символов. Она передаётся при создании процесса, после обычного PowerShell-профиля и выбора папки, а не вставляется в терминал через буфер обмена. Для cmd нужна одна строка; несколько команд можно соединить через `&&`. Он запускается с `/d`, отключающим AutoRun из реестра, а `/s /k` сохраняют внутренние кавычки команды и оставляют оболочку открытой — [параметры cmd](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/cmd).

Пример профиля: название **TerminalV — разработка**, оболочка **Windows PowerShell**, папка `C:\Repos\TerminalV`, зелёная метка, команда `muse`. Команда выполняется только при явном запуске или перезапуске этой сессии. Вводите только доверенные команды. Профили и их копии в сессиях сохраняются в локальной SQLite-базе без шифрования: не записывайте в стартовую команду пароли или токены.

- Закрытие и повторное открытие окна, обновление приложения и возврат скрытой вкладки подключаются к живому процессу без повторного выполнения команды.
- Если процесс профильной сессии отсутствует, в том числе после перезагрузки Windows, сохраняется экран и предлагается явный запуск. Кнопка предупреждает о стартовой команде; её текст доступен в подсказке кнопки.
- Перезапуск использует сохранённую в сессии оболочку и команду, а также последний известный каталог. Новая сессия из профиля использует папку самого профиля. Для cmd изменение текущей папки пока не отслеживается.
- Редактирование и удаление профиля не меняют работающие сессии. **Удалить…** требует подтверждения и удаляет только профиль.
- **Новая вкладка** и `Ctrl+Shift+T` сохраняют прежнее поведение: обычная оболочка в папке активной сессии, без наследования её стартовой команды.
- **Закрыть без изменений** и `Esc` отменяют несохранённые правки. Уже сохранённые параметры остаются в базе.

После обновления может оставаться фоновый процесс старой версии. Он продолжает обслуживать существующие вкладки, но запуск профиля будет явно отклонён. Сначала сохраните работу и завершите нужные задачи, затем перезагрузите Windows для обновления фонового процесса. TerminalV сам не прерывает рабочие сессии.

### Быстрый запуск WSL

Реализовано в исходниках после v0.5.1. Откройте **Профили и оболочки → Ubuntu** (или другой установленный дистрибутив). TerminalV запускает системный `wsl.exe --distribution Ubuntu --cd ~` прямо в новой вкладке, без промежуточного PowerShell и без ручного создания профиля. Открывается домашняя папка пользователя Linux и стандартная оболочка выбранного дистрибутива.

Список читается асинхронно через `wsl --list --quiet`, с ограничением в 5 секунд. Эта операция не запускает и не устанавливает Linux. Кнопка **Обновить список оболочек и WSL** повторяет обнаружение после установки дистрибутива. При недоступности WSL появляется пояснение; сохранённые профили остаются доступны. Служебные дистрибутивы Docker Desktop и Rancher Desktop не показываются, как в Windows Terminal.

Имя дистрибутива сохраняется вместе с сессией. Повторное открытие окна подключается к живой сессии без повторного запуска. Если процесс отсутствует, вывод сохраняется и нужен явный **Перезапустить**. Старый фоновый процесс, не поддерживающий WSL, выдаёт ошибку и не запускает другую оболочку вместо Linux; обновление хоста требует сохранения работы и перезагрузки Windows.

В этой реализации WSL запускается в домашней папке Linux: отслеживание Linux cwd, стартовые команды и настройка WSL в редакторе профилей пока не предусмотрены. Обычная новая вкладка или разделение из WSL запускает стандартную Windows-оболочку без наследования Linux-пути. Встроенное меню не устанавливает, не перенастраивает и не останавливает дистрибутивы.

Синтаксис: [команды WSL](https://learn.microsoft.com/en-us/windows/wsl/basic-commands). Имя передаётся без кавычек после проверки допустимых символов: WSL воспринимает кавычки как часть имени ([WSL #9792](https://github.com/microsoft/WSL/issues/9792)). Фильтрация служебных дистрибутивов соответствует [генератору WSL-профилей Windows Terminal](https://github.com/microsoft/terminal/blob/main/src/cascadia/TerminalSettingsModel/WslDistroGenerator.cpp).

## Разделение экрана на панели

Доступно с v0.5.0. Вверху терминала находятся кнопки **Справа**, **Снизу** и **Отделить**.

- **Справа** (`Alt+Shift+=`) делит активную панель вертикальным разделителем на две соседние панели.
- **Снизу** (`Alt+Shift+-`) делит её горизонтальным разделителем на верхнюю и нижнюю.
- В новой панели запускается обычная оболочка в последней известной папке активной сессии. Она наследует группу проекта, но не стартовую команду, выбранную оболочку профиля, цвет или закрепление. Например, разделение панели с Muse не запускает вторую копию Muse.
- Можно делить любую панель повторно, сочетая оба направления. В одном расположении допускается до восьми панелей. При недостатке места кнопка разделения недоступна; существующее расположение при уменьшении окна сохраняется.

Каждая панель остаётся самостоятельной сессией в боковом списке. Нажатие на сессию показывает всё её расположение и выбирает соответствующую панель. Обычная **Новая вкладка** и запуск профиля открываются отдельно. Группы проекта и расположения панелей независимы: перетаскивание строки в боковом списке меняет порядок/группу, но не геометрию панелей.

### Фокус и размеры

Кликните внутри панели или по её заголовку; выбранная панель отмечена акцентной рамкой и выделена слева. `Alt+←`, `Alt+→`, `Alt+↑`, `Alt+↓` переводят фокус к соседней панели в соответствующем направлении. Разделение использует физические клавиши `=` и `-`, поэтому работает и с русской раскладкой; удержание сочетания не создаёт несколько сессий.

Ввод, `Ctrl+C`, копирование и вставка относятся только к выбранной панели. Правый клик для вставки выбирает панель под указателем. Поиск `Ctrl+Shift+F` также относится к выбранной сессии; переход к другой панели закрывает поиск. Поля поиска и диалоги не передают сочетания разделения в терминал. Сигнал BEL от видимой, но неактивной панели обрабатывается как сигнал другой неактивной сессии: подсветка/звук без перехвата фокуса.

Перетащите разделитель мышью. Для изменения размера клавиатурой перейдите к нему через `Tab`: стрелки в направлении изменения размера двигают его, `Home` / `End` — к допустимым краям. Изменённые размеры передаются всем затронутым терминалам, включая неактивные видимые панели и полноэкранные TUI. Масштаб и изменение размеров окна также пересчитывают все видимые панели.

### Закрытие, скрытие и восстановление

- Крестик в заголовке панели, крестик её сессии слева и `Ctrl+Shift+W` используют общее подтверждение. Завершается только выбранная сессия; соседние остаются работать и занимают освободившееся место. Завершённый процесс удаляется без дополнительного подтверждения.
- **Отделить** показывает активную панель отдельно и убирает её из прежнего расположения. Процесс и экран сохраняются, остальные панели остаются связанными. Объединение уже существующих отдельных сессий в этой версии не предусмотрено.
- **Скрыть, оставив работать** убирает панель из расположения, сохраняя её процесс. Возврат из списка скрытых открывает эту сессию отдельно.
- При закрытии окна расположения, пропорции и активная панель сохраняются вместе со списком сессий в SQLite. Следующее открытие подключается к прежним процессам без повторного запуска команд профиля.
- После перезагрузки Windows расположение остаётся прежним; обычные видимые сессии запускаются заново, профильные и скрытые требуют явного запуска, как и раньше. Повреждённые/устаревшие ссылки расположения не удаляют сохранённые сессии: они открываются отдельно.

## Клавиши

| Сочетание | Действие |
|-----------|----------|
| `Ctrl+Shift+T` | Новая вкладка в папке активной сессии |
| `Ctrl+Shift+W` | Закрыть текущую вкладку |
| `Alt+Shift+=` | Новая панель справа |
| `Alt+Shift+-` | Новая панель снизу |
| `Alt+←` / `→` / `↑` / `↓` | Выбрать соседнюю панель |
| `Ctrl+Tab` | Следующая вкладка |
| `Ctrl+Shift+Tab` | Предыдущая вкладка |
| `Alt+1` … `Alt+9` | Вкладка по номеру |
| `Ctrl+C` | Копировать выделение (если есть), иначе сигнал прерывания в оболочку |
| `Ctrl+V` / `Ctrl+Shift+V` | Вставить |
| `Ctrl+Shift+C` | Копировать выделение |
| `Ctrl+Alt+C` | Копировать видимые строки отдельно, без пробелов по краям |
| `Ctrl+Shift+F` | Поиск по выводу текущей вкладки |
| `Ctrl+,` | Оформление |
| `Ctrl+B` | Свернуть / показать список сессий |
| `Ctrl+` `+` / `-` / `0` | Масштаб терминала, как в VS Code |
| `Ctrl` + колесо | Масштаб терминала |

Правый клик по терминалу: при выделении — копировать, иначе — вставить.

При вставке текста с переносами строк открывается предупреждение с предпросмотром и кнопками **Вставить в любом случае** / **Отмена**.
Это работает для `Ctrl+V`, `Ctrl+Shift+V` и правого клика; перенос строки в конце команды тоже требует подтверждения.
`Esc` отменяет вставку. После подтверждения вставляется текст, показанный в предпросмотре, даже если буфер обмена уже изменился. У длинных текстов предпросмотр сокращён; вставляется весь текст.

Если скопированные команды склеиваются с большими промежутками, выделите их и нажмите `Ctrl+Alt+C`.
Этот режим берёт строки прямо с экрана, убирает пробелы по краям каждой строки и сохраняет переносы и пустые строки.
Длинная команда, перенесённая на экране, тоже будет разделена на строки. Для кода с отступами или длинных команд используйте обычный `Ctrl+C` / `Ctrl+Shift+C` — они сохраняют отступы и склеивают автоматические переносы xterm.

## Поиск по выводу

Нажмите `Ctrl+Shift+F` или кнопку поиска слева вверху. Сочетание работает и при русской раскладке.
Панель появляется над терминалом и ищет текст в доступном буфере текущей вкладки, включая историю прокрутки. В полноэкранных TUI доступен только текст текущего буфера, а не исчезнувшие ответы приложения.

- Совпадения подсвечиваются, рядом с полем отображается номер выбранного результата и количество совпадений.
- `Enter` / `Shift+Enter` в поле поиска или кнопки `↓` / `↑` — следующее / предыдущее совпадение, с переходом по кругу.
- `Aa` включает учёт регистра. По умолчанию регистр не учитывается; специальные символы ищутся как обычный текст, не как регулярное выражение.
- `Esc` или крестик закрывают поиск и возвращают фокус терминалу. Переключение вкладки или открытие настроек также закрывает поиск.
- При новом выводе и изменении размеров результаты обновляются. Автоматический перенос длинной строки не разрывает искомое слово.
- Для отзывчивости интерфейса подсвечиваются первые 1000 совпадений; счётчик показывает `≥1000`, а переход между результатами доступен и за пределами этого лимита.

Ввод, вставка текста и сочетания клавиш внутри панели поиска не отправляются в оболочку. Текст запроса сохраняется между открытиями панели в пределах текущего запуска приложения, но не записывается в базу данных.

## Уведомления

Если программа выводит терминальный сигнал **BEL**, неактивная вкладка получает подсветку и отметку **Сигнал**. Звучит системный сигнал Windows; активная вкладка не подаёт ни звук, ни отметку, в том числе когда само окно не в фокусе. Обычный новый вывод по-прежнему отмечается индикатором активности — это не звуковое уведомление.

- В **⋯ → Без звука для этой сессии → Сохранить** можно отключить звук выбранной сессии. Подсветка остаётся. Настройка переживает закрытие окна, скрытие и восстановление вкладки; новые сессии создаются со звуком.
- Отметка **Сигнал** снимается при открытии соответствующей вкладки. Уведомление не переключает вкладку, не открывает диалог и не забирает фокус у поиска или ввода.
- Для скрытых сессий кнопка **Скрытые · N** подсвечивается и показывает `!` с количеством сессий, ожидающих внимания. Откройте список, чтобы увидеть источник сигнала.
- Звук ограничен одним сигналом за 2 секунды для всего приложения и одним за 5 секунд для каждой сессии. Повторы не ставятся в очередь; визуальная отметка остаётся, даже если звук ограничен или отключён.
- При восстановлении окна история вывода не воспроизводит старые сигналы и не создаёт отметок заново. Пока окно полностью закрыто, звуковые уведомления не воспроизводятся; сами фоновые сессии продолжают работать.

Для ручной проверки запустите команду и до окончания паузы переключитесь на другую вкладку:

```powershell
Start-Sleep -Seconds 3; [Console]::Write([char]7)
```

Это именно сигнал BEL в выводе терминала. Вызов `[Console]::Beep()` и другие звуки, которые программа воспроизводит напрямую через Windows, не проходят через эту настройку и не могут быть ею заглушены. Завершение команды и ожидание ответа Muse пока не определяются автоматически: без достоверного события приложение не пытается угадывать их по тексту вывода.

## Рабочая папка

После `cd` нажмите **Новая вкладка** или `Ctrl+Shift+T`: новая сессия откроется в текущей папке активного PowerShell. Сочетание работает в английской и русской раскладках. Поддерживаются пути с пробелами, кириллицей и буквальными квадратными скобками; переименование вкладки на путь не влияет.

Windows PowerShell 5.1 и PowerShell 7 сообщают путь при показе приглашения через [OSC 9;9](https://learn.microsoft.com/en-us/windows/terminal/tutorials/shell-integration). Интеграция подключается при запуске оболочки после обычных профилей, не меняя их файлы и внешний вид существующего приглашения. При явно заданной папке (наследование, восстановление, перезапуск) она выбирается после загрузки профилей. В первой сессии без сохранённого пути профиль по-прежнему может выбрать стартовую папку сам.

Последний известный каталог сохраняется отдельно от заголовка и экранного буфера, в том числе пока окно закрыто. Восстановление подключается к прежнему процессу и получает его актуальный путь; после перезагрузки Windows обычная видимая сессия запускается в сохранённой папке. Скрытые и профильные сессии без живого процесса требуют явного запуска. **Перезапустить** также использует последний известный каталог.

Если выбранная папка удалена или недоступна, обычная сессия открывается в домашней папке, а над её терминалом появляется уведомление с кнопкой закрытия. Для профилей вместо этого отменяется запуск. Ввод уведомление не блокирует. Пока путь неизвестен, используется стартовая папка сессии. Внутри Muse и других запущенных программ, в провайдерах вроде `HKCU:` и в оболочках без такой интеграции остаётся последний известный файловый каталог. Если в уже открытой оболочке полностью переопределить функцию `prompt`, автоматическое обновление пути прекратится до запуска новой сессии.

После обновления старый фоновый процесс может продолжать обслуживать работающие сессии прежним кодом. В этом случае TerminalV показывает предупреждение: для включения отслеживания пути завершите нужные задачи и перезагрузите Windows. Простое закрытие окна сохраняет фоновый процесс и потому его не обновляет. TerminalV не завершает такие сессии автоматически и не отправляет в них команды установки интеграции.

## Если процесс завершился

На панели появляется код выхода и кнопка **Перезапустить**. Буфер экрана при этом сохраняется до рестарта.

## Оформление

`Ctrl+,` или иконка регуляторов слева открывает меню: тема (тёмные и две светлые), шрифт, размер, фото на фон сессий и его прозрачность. Список сессий сворачивается кнопкой с иконкой боковой панели или `Ctrl+B`.

В обновлённом оформлении v0.5.1 вверху терминала показываются название и последний известный каталог активной сессии; длинный путь целиком доступен при наведении. Темы представлены миниатюрами интерфейса с отметкой выбранной. `Tab` остаётся внутри настроек, `Esc` закрывает их и возвращает фокус терминалу. Сочетания управления сессиями за открытыми настройками не выполняются.

Сессии живут в фоновом процессе `TerminalV.exe --host`. Закрытие окна не убивает vim, k9s и другой TUI — при следующем открытии вкладка подключается к тому же PTY. После перезагрузки Windows остаётся снимок экрана (включая alt-screen) в SQLite: обычная видимая сессия стартует заново, скрытая или профильная требует явного запуска.

Полоса прокрутки терминала появляется, только если буфер не помещается, и окрашена в цвета текущей темы.

## Обновление

Установленная копия при запуске спрашивает GitHub. Если есть тег новее, слева появляется **Обновить**. Тот же механизм с командной строки:

```powershell
TerminalV update --check
TerminalV update
```

Команда идёт в консольный `TerminalV.com` (как `mining-fleet update`), а окно — это `TerminalV.exe`. PowerShell ждёт `.com` и возвращает приглашение. Открытое окно после установки закрывается и запускается заново.

Клик по `v…` внизу сайдбара повторяет проверку. Debug-сборка из `bin\Debug` / `bin\Release` не обновляется сама.

Перед заменой EXE проверяются SHA-256, формат ZIP и запуск `--help`; неудачный запуск после замены откатывает EXE.

С v0.5.0 дополнительно проверяется наличие `TerminalV.exe`, `TerminalV.com` и `wwwroot/index.html`. Консольный запускатель обновляется вместе с интерфейсом; блокировка файла сохраняет отложенное обновление для повторной попытки при следующем запуске. Пока одно обновление ожидает применения, повторная установка отклоняется.

Сборка 0.1.0 этого не умеет — поставьте 0.2.0 через `irm` один раз.

## Требования

- Windows 10 1809+ / Windows 11, x64
- Microsoft Edge WebView2 Runtime (обычно уже установлен)
