# TerminalV v0.6.18 — высота элементов сессий

## Новое

- Настройки → «Панель сессий» → «Высота элемента сессии»: Стандарт / Минимальная.
- Минимальная: строка 36px вместо 58px, скрыта вторая строка (мета-информация), ужаты кнопки. Настройка хранится в базе и переживает перезапуск.

## Проверки

- `ui/tests/chrome.test.js`: 3/3 pass; рядом `tui-scroll`, `paste-file`, `write-chunk` — всего 18/18 pass; `node --check` чистый.
- Windows-сборка — через GitHub Actions при публикации тега (C#: одно поле `AppSettings.SessionDensity`, старые сохранения совместимы).
