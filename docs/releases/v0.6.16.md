# TerminalV v0.6.16 — тихий свёрнутый сайдбар

## Изменено

- Кнопка управления сессией («…» на строке вкладки) в свёрнутом сайдбаре больше не показывается вообще: ни на активной вкладке, ни при наведении. Раньше она всплывала оверлеем поверх узкой (56px) колонки.
- Управление сессией доступно как раньше: правый клик по вкладке, Shift+F10, в развёрнутом сайдбаре — кнопка на месте.

## Проверки

- `ui/tests/chrome.test.js`: 2/2 pass (запрет показа + отсутствие re-show правил).
- Windows-сборка — через GitHub Actions при публикации тега.
