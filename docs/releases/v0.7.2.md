# TerminalV v0.7.2 — выключатель WebGL-рендеринга

## Новое

- Настройки → «Рендеринг» → «Аппаратный рендеринг (WebGL)», по умолчанию включён. Выключение снимает WebGL со всех вкладок (возврат на canvas-рендер xterm) — обход чёрных полос tearing на быстро переписываемых строках (тикающие таймеры muse + полноширинные пробелы). Выбор хранится в базе.

## Проверки

- Новый `ui/tests/hardware-rendering.test.js`: 3/3 pass; весь UI-сьют 33/33 pass; `node --check` чистый.
- Windows-сборка — через GitHub Actions при публикации тега.
