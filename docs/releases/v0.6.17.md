# TerminalV v0.6.17 — колесо мыши для mouse-TUI

## Исправлено

- В TUI-сессиях (`tui-lock`) колесо мыши больше не глушится, если приложение включило mouse-режим: xterm пересылает колесо как mouse-репорты, и свой скроллбар крутит само приложение (проверено на жалобе про Grok Build — полноэкранный mouse-TUI).
- Остальные TUI (клавиатурные, без мыши) — как раньше: вьюпорт прибит, скролл терминала не уезжает. Зум по Ctrl+колесо — без изменений.

## Проверки

- Новый `ui/tests/tui-scroll.test.js`: 4/4 pass (матрица lock/mouse/zoom); рядом `chrome`, `paste-file`, `write-chunk` — всего 17/17 pass; `node --check` чистый.
- Windows-сборка — через GitHub Actions при публикации тега. Живая проверка колеса — на стороне пользователя в сессии grok.
